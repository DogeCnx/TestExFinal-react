import React from 'react';

function LibraVsFram(props) {
    return (
        <div>
            <h1>
                library และ  frameworks ต่างกันตรงที่   library เป็น dependencies 
                ส่วน  frameworks เป็น package manager ช่วยจัดการโค้ดให้เรา
            </h1>
        </div>
    );
}

export default LibraVsFram;